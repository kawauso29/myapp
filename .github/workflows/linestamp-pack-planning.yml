name: Linestamp Pack Planning
on:
  workflow_dispatch:
  schedule:
    - cron: '0 1 * * *'  # 毎日 10:00 JST

jobs:
  pack-planning:
    runs-on: [self-hosted, linestamp]
    permissions:
      contents: read
      issues: write
    steps:
      - uses: actions/checkout@v4

      - name: Run pack planning
        run: |
          echo "Linestamp pack planning workflow triggered"
          echo "TODO: Implement pack planning automation"
